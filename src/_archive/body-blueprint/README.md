# Archived: Body Blueprint page v1

`page.v1.tsx` is the pre-refinement version of The Body Blueprint™ page,
including the original hero video section (video-frame with play button,
captioned "The Body Blueprint™, Explained — Emmy Branger" and subtitle
demo quote).

The video section was archived (not deleted) and replaced with an
editorial pull-quote placeholder until the final Body Blueprint™ video
is ready — at that point, restore the `video-frame` block from this file
in place of the placeholder.

All written content and page structure otherwise remained the same; only
subtle background decorative graphics (arcs, rings, dotted grids,
crosshairs) were added on top of the existing layout.
