# Archived: Systems page v1

`page.v1.tsx` is the original Systems page (v1.0/v1.1 content pass), before
the purely-visual "editorial luxury" enhancement pass that added subtle
decorative graphics (arcs, rings, dotted grids, crosshairs) around the
existing sections.

No copy changed and the System Cards (`.systems-grid` / `.system-card`)
were never modified — this archive exists only per the "archive, don't
delete" workflow rule.
